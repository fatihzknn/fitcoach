package com.fitcoach.profile.domain;

/** Training experience level (Step 2). */
public enum TrainingBackground {
    STARTING,   // just starting
    RETURNING,  // trained before, returning
    REGULAR     // trains regularly
}
