package com.fitcoach.profile.domain;

/** Reported pain or injury limitation (Step 6). NONE means no limitations. */
public enum PainArea {
    NONE,
    KNEE,
    LOWER_BACK,
    SHOULDER,
    OTHER
}
